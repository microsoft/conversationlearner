version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.11.2-browsers
    steps:
      - checkout
      - run: npm install
      - run: npm run test:prod 
      - run: npm run build
workflows:
  version: 2
  build:
    jobs:
      - build